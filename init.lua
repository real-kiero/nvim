-- load options (vim options)
-- options has to be loaded before config.lazy
require("options")
-- lazy.nvim package manager (structured)
-- I prefer lazy to packer
require("config.lazy")
-- load mappings
require("mappings")
